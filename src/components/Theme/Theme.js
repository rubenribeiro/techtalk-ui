import { createMuiTheme } from '@material-ui/core/styles';

export default createMuiTheme({
    typography: {
        tab: {
            fontFamily: "Pacifico",
            textTransform: "none",
            fontWeight: "700",
            fontSize: "1rem",
        }
    },
    palette: {
        background: {
            default: '#eef0f1'
        }
    }
});